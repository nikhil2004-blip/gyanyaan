# Backend API URL (change to your Render/Railway URL in production)
VITE_API_URL=http://localhost:3000

# Google OAuth Client ID (same one used in the backend).
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
